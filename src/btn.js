export const onClickBtn = () => {
  throw new Error(
    "Don't worry. This button is supposed to throw an error!!! By using devtool: inline-source-map in webpack settings so you can track where the error is coming from."
  );
};
